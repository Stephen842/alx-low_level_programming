#include <stdio.h>

int main(void)
{
	int a;
	char b;
	float c;

	printf("Address of the variable 'a': %p\n", &a);
	printf("Address of the variable 'b': %p\n", &b);
	printf("Address of the variable 'c': %p\n", &c);
	return (0);
}
